// Importing the 'handlers' object from the 'auth.ts' file located in the '@/auth' directory.
// This file likely defines various handler functions for authentication purposes.
import { handlers } from '@/auth'

// Destructuring the 'handlers' object to extract the 'GET' and 'POST' properties.
// These are likely functions handling GET and POST requests respectively, in the context of authentication.
export const { GET, POST } = handlers
